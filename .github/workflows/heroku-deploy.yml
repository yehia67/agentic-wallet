name: Deploy to Staging

on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened, closed]

jobs:
  deploy-staging:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 8

      - name: Install Heroku CLI
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        run: |
          curl https://cli-assets.heroku.com/install.sh | sh
          
      - name: Deploy to Staging Environment
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        env:
          HEROKU_API_KEY: ${{ secrets.HEROKU_API_KEY }}
        run: |
          # Debug: Check if API key is available
          echo "Checking HEROKU_API_KEY..."
          if [ -z "$HEROKU_API_KEY" ]; then
            echo "❌ HEROKU_API_KEY is empty or not set!"
            echo "Available env vars:"
            env | grep -i heroku || echo "No HEROKU env vars found"
            exit 1
          else
            echo "✅ HEROKU_API_KEY is set (length: ${#HEROKU_API_KEY})"
          fi
          
          # Set up Heroku authentication using .netrc (most reliable method)
          echo "machine api.heroku.com login _ password $HEROKU_API_KEY" > ~/.netrc
          echo "machine git.heroku.com login _ password $HEROKU_API_KEY" >> ~/.netrc
          chmod 600 ~/.netrc
          
          # Verify authentication works
          echo "Testing Heroku authentication..."
          heroku auth:whoami
          
          # Add Heroku remote for staging app
          git remote add heroku https://git.heroku.com/agentic-wallet.git
          
          # Deploy to staging environment via staging-pipline
          git push heroku main
          
          # Check staging deployment status
          heroku releases -a agentic-wallet --limit 1
          
      - name: Notify staging deployment success
        if: success() && github.ref == 'refs/heads/main'
        run: |
          echo "✅ Staging deployment successful!"
          echo "🌐 Staging URL: https://agentic-wallet.herokuapp.com"
          echo "📊 Pipeline: staging-pipline"
          echo "🚀 Ready for testing in staging environment"
        
      - name: Notify staging deployment failure
        if: failure() && github.ref == 'refs/heads/main'
        run: |
          echo "❌ Staging deployment failed!"
          echo "Check the logs above for details."
          echo "🔧 Fix issues before promoting to production"

  review-app-info:
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    
    steps:
      - name: PR Review App Info
        run: |
          echo "🔍 Review app will be automatically created by Heroku"
          echo "📝 PR #${{ github.event.pull_request.number }}"
          echo "🌐 Check your Heroku pipeline for the review app URL"